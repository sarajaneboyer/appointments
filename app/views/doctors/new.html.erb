<h1>Add a Doctor</h1>
<%= form_with(model: @doctor, local: true, class: "ui form") do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Doctor's Last Name" %>
  </div>
  <%= f.submit nil, class: "ui green button inverted" %>
  <% end %>